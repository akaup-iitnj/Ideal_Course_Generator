# Optional: exact public URL to show in the "Share" strip (your ngrok / Cloudflare tunnel URL).
# If unset, the app guesses from the browser request when not using localhost.
# STAGE5_PUBLIC_URL=https://your-subdomain.ngrok-free.app
